export default function ExamsPage() {
    return (
        <main style={{ minHeight: "60vh", display: "flex", alignItems: "center", justifyContent: "center", flexDirection: "column", gap: 12 }}>
            <h1 style={{ fontSize: "2rem", fontWeight: 800 }}>Mock Exams</h1>
            <p style={{ color: "var(--text-muted)" }}>Coming soon — practice tests and exam simulations.</p>
        </main>
    );
}
